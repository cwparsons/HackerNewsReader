import {
	FONT_SIZE,
	LINE_HEIGHT,
	pt,
	WHITE
} from '../../Configuration/globalStyles';

export const tagsStyles = {
	p: {
		color: WHITE,
		fontSize: FONT_SIZE,
		lineHeight: LINE_HEIGHT,
		marginBottom: pt(2),
		marginTop: 0
	},
	a: {
		color: WHITE
	}
};
