import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
	row: {
		alignItems: 'center',
		flexDirection: 'row',
		height: 100,
		justifyContent: 'center'
	},
	activityIndicator: {
		color: 'orange'
	}
});
